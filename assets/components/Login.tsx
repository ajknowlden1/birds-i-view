import React from "react"
import {View, Text, StyleSheet, TouchableOpacity, Image, Dimensions, ScrollView, FlatList, Alert} from "react-native"

export default function Login() {
    return (
        <View>
            <Text>Login</Text>
        </View>
    );
}
